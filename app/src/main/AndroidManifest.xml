<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:name=".FileBridgeApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locale_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FileBridge"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FileBridge">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            FTP server foreground service.
            • foregroundServiceType="dataSync" — appropriate for ongoing user-initiated file
              transfers. Declared on the element (required on API 29+) and also passed to
              startForeground() at runtime via ServiceCompat for API 34+ enforcement.
            • exported="false" — only started via explicit Intent from within the app.
        -->
        <service
            android:name=".service.FtpForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!--
            Receives notification action buttons (Stop). Not exported — only the app's own
            PendingIntents target it. Using a receiver (rather than a getService PendingIntent)
            makes the Stop button reliable from the background notification shade.
        -->
        <receiver
            android:name=".service.ServerActionReceiver"
            android:exported="false" />

        <!--
            Boot receiver — starts the server on device boot when "Auto-start on boot" is enabled.
            exported="true" is required for system-delivered boot broadcasts.
            directBootAware="false" means the receiver fires after the user has unlocked the device
            (we need credential storage, which is encrypted and only available after unlock).
        -->
        <receiver
            android:name=".receiver.BootCompletedReceiver"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--
            Home-screen widget receiver.
            The meta-data points to the AppWidget provider info XML.
        -->
        <receiver
            android:name=".widget.FtpServerWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/ftp_server_widget_info" />
        </receiver>

    </application>

</manifest>
