package com.aionyxe.filebridge.ui.settings

/** Identifies which settings field carries a validation error. */
enum class SettingsField {
    FTP_PORT,
    PASV_MIN,
    PASV_MAX,
    USERNAME,
    PASSWORD,
}
