package com.aionyxe.filebridge.ui.theme

import androidx.compose.material3.Typography

/** Material 3 typography using the system default font. Only body sizes are overridden. */
val FileBridgeTypography = Typography()
